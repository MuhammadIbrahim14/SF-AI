name: sie_camera_hand_cursor
description: Disposable SIE technology spike — camera + hand landmarks + virtual cursor (Web + Android only).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.12.0+2
  hand_landmarker: ^3.0.1
  permission_handler: ^12.0.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
