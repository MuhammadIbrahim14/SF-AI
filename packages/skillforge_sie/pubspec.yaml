name: skillforge_sie
description: SkillForge AI Spatial Interaction Engine — production package (Approach A).
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.11.5
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  permission_handler: ^12.0.1
  camera: ^0.12.0+2
  hand_landmarker: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: false
